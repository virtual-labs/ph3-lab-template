name: test-deploymnet

on:
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  call-workflow-test:
    uses: virtual-labs/vlabs-workflows/.github/workflows/testph3lab.yml@test
    secrets: inherit
